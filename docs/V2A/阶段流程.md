# 电气连接
J9 板载驻极体 MIC
    │
    ├── MIC2P ── C73 1uF ──> ES8388 LIN2，pin 22
    │
    └── MIC2N ── C74 1uF ──> ES8388 RIN2，pin 21
```
              VCC3V3_SYS
                   │
          board bias network
                   │
                   v
        Onboard Electret MIC J9
             │           │
           MIC2P       MIC2N
             │           │
           C73 1uF     C74 1uF
             │           │
             v           v
        ES8388 LIN2   ES8388 RIN2
             \           /
              \         /
             Analog routing/PGA
                    │
                   ADC
                    │
             ES8388 Digital DAI
                    │
                   I2S
                    │
          RK3588 I2S Controller
                    │
                   DMA
                    │
              Linux ASoC
                    │
              ALSA PCM Device
                    │
                alsa-lib
                    │
             V2A Application
```

数据流
```
snd_pcm_open(hw:...)
        ↓
snd_pcm_hw_params_any()
        ↓
set RW_INTERLEAVED
        ↓
set format
        ↓
set channels
        ↓
set rate
        ↓
set period
        ↓
set buffer
        ↓
snd_pcm_hw_params()
        ↓
read back ACTUAL parameters
        ↓
snd_pcm_sw_params_current()
        ↓
avail_min
        ↓
timestamp enable
        ↓
timestamp type
        ↓
snd_pcm_sw_params()
        ↓
snd_pcm_prepare()
        ↓
snd_pcm_readi()
        ↓
PCM frames
        ├── WAV/raw
        ├── peak/RMS/clipping
        ├── cumulative_frames
        └── CSV
```

与视频混合接口

```
V2 Camera

frame_index
v4l2_sequence
v4l2_timestamp
timestamp_type
timestamp_source
dq_monotonic_ns
        │
        │
        ├────────────┐
        │            │
        v            v

               Common clock analysis

        ^            ^
        │            │
        ├────────────┘
        │
V2A Audio

audio_chunk_id
frames_read
cumulative_frames
alsa_status_timestamp
alsa_trigger_timestamp
app_monotonic_timestamp
nominal_sample_time

        ↓

V8 only:

offset estimation
→ drift estimation
→ synchronization policy
→ encode
→ mux
```

```
声音
 ↓
MIC
 ↓
模拟电压
 ↓
ES8388
 ├─ PGA / Mixer
 └─ ADC
 ↓
PCM samples
 ↓
I2S
 ↓
RK3588 I2S RX
 ↓
DMA
 ↓
RAM
 ↓
arecord
 ↓
WAV
```

ALSA Card 3
    ↓
rockchip-es8388

[已确认]
Card 3 / Device 0
    ↓
同时有 playback + capture

[已确认]
arecord hardware capture
    ↓
hw:3,0

capture PCM = hw:3,0

native channel count = 2
native formats = S16_LE / S24_LE
native rate range = 8 kHz ~ 96 kHz
normal read() access = RW_INTERLEAVED

# 实验参数
F — MVP FORMAT
────────────────────────
Device       hw:3,0
Access       RW_INTERLEAVED
Format       S16_LE
Rate         48000 Hz
Channels     2


G — PCM BUFFER BASELINE
────────────────────────
Period       1024 frames
Buffer       4096 frames
Periods      4

Period time  ≈ 21.3 ms
Buffer time  ≈ 85.3 ms
Period bytes = 4096 B
Buffer bytes = 16384 B

ES8388
不停地产生 PCM
        ↓
I2S
        ↓
DMA
        ↓
┌─────────────────────┐
│ ALSA ring buffer    │
│                     │
│ PCM PCM PCM PCM...  │
└─────────────────────┘
        ↓
你的程序周期性读取

