# YOLO 后处理
9 × RKNN INT8 变换张量  
│  
├── 分支 0：box / class / score-sum  
├── 分支 1：box / class / score-sum  
└── 分支 2：box / class / score-sum  
│  
▼  
验证张量索引/形状/布局/零点/缩放  
▼  
变换反量化  
float = (量化值 - 零点) × 缩放因子  
▼  
DFL softmax 和期望距离  
▼  
解码网格 + 步长  
▼  
模型空间候选框  
▼  
置信度阈值  
▼  
类别过滤：class_id == 1  
▼  
xyxy 验证  
▼  
类感知 NMS  
▼  
最大检测数量

Rockchip 官方代码的 DFL 解码会对四个边分别做 softmax 期望值，再结合 grid 和 stride 构造框；之后按置信度排序并按类别执行 NMS。

# 预处理流
相机源图像帧  
4128×1908 或实际 V2 模式  
│  
▼  
均匀缩放 = min(模型宽度/源宽度，模型高度/源高度)  
│  
▼  
调整大小的内容 + 居中填充  
│  
▼  
960×960 模型坐标  
│  
▼  
x_unpadded = x_model - pad_left  
y_unpadded = y_model - pad_top  
│  
▼  
x_source = x_unpadded / uniform_scale  
y_source = y_unpadded / uniform_scale  
│  
▼  
裁剪至源图像  

# 6.3 多线程 Pipeline

CameraThread
  DQBUF
  copy/convert to application-owned memory
  QBUF
     │ FramePacket
     ▼
BoundedLatestQueue<FramePacket>, capacity 1..3
     │ drop oldest when full
     ▼
InferenceThread
  preprocess
  RKNN input/run/output
  copy outputs or decode before output release
     │ InferencePacket
     ▼
BoundedLatestQueue<InferencePacket>, capacity 1..3
     ▼
PostprocessThread
  decode/filter/NMS/map/select/state/error
     │ TargetError
     ▼
MockControlSink
  CSV + telemetry + rate-limited log


V4 → V5 ControlSink 替换
V4:
TargetError → IControlSink → MockControlSink

V5:
TargetError → IControlSink → UartControlSink
                              │
                              ▼
                      ProtocolEncoder
                              ▼
                           UART TX