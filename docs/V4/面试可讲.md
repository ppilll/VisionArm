面试可讲点

可以形成完整工程叙事：

为什么 YOLOv8 RKNN 输出不是最终 xyxy。
如何根据 tensor shape、stride、量化参数冻结后处理契约。
INT8 affine 反量化和 DFL decode。
confidence filtering、class-aware NMS。
model、letterbox、Camera 三种坐标空间的转换。
为什么控制误差必须基于原始 Camera 中心。
多足球唯一目标选择与连续性评分。
acquire/lost hysteresis 和 stale result 的区别。
V4L2 mmap buffer 在 DQBUF/QBUF 之间的所有权。
RKNN output buffer 在 get/release 之间的所有权。
producer/consumer、bounded queue、latest-frame-wins 和 backpressure。
为什么 30 FPS Camera 配 10 FPS inference 不能处理完整历史。
多线程 graceful shutdown。
p50/p95/p99 latency 与平均延迟的区别。
为什么保留独立 Camera、Audio 和 RKNN 测试工具。
为什么 V4 只是实时控制输入闭环骨架。
如何通过 IControlSink 让 V5 替换出口而不改感知链路。