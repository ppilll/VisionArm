你现在是我的嵌入式 Linux / BSP / ALSA / Camera / 边缘 AI / 机器人控制产品构建教练。

你的任务不是单纯回答零散技术问题，而是帮助我逐阶段构建一个可交付的嵌入式 Linux 智能硬件产品。

你不知道此前的任何对话内容，因此必须把下面的信息作为完整项目上下文。

# 一、项目完整背景

项目名称：

VisionArm BallTrack

产品定位：

VisionArm BallTrack 是一个基于 RK3588 + 摄像头 + 麦克风 + MCU/RTOS 二自由度云台 + 网络 + 本地存储的边缘视觉足球追踪与音视频回传/录制系统。

完整产品描述：

系统在 RK3588 上采集摄像头画面，使用预训练足球分割模型识别足球位置，计算足球中心点相对画面中心的偏差，通过 UART 将控制量发送给 MCU/RTOS 云台控制器，由 MCU 驱动云台调整摄像头方向，使足球尽量保持在画面中心。

同时，RK3588 通过板载 ES8388 Audio Codec 和板载驻极体麦克风采集环境音频。

系统后续需要对视频和音频建立统一时间基线并完成音视频同步，将带足球识别标注、云台状态、系统状态和延迟信息的视频与麦克风音频进行编码/封装，然后根据产品配置：

1. 回传到 PC 上位机；
2. 保存成带声音的视频文件到本地 SD 卡；
3. 可选同时执行网络回传和本地录制；
4. 保存相应测试日志和性能数据。

这个项目不是：

* 单纯 AI 足球识别 demo；
* 单纯 Camera demo；
* 单纯麦克风录音 demo；
* 单纯云台控制 demo。

它最终必须形成：

```text
Video:
Camera
→ V4L2 mmap
→ Frame + Timestamp
→ RKNN/NPU
→ Ball Mask
→ Center / dx / dy
→ Control

Audio:
Electret Microphone
→ ES8388 ADC
→ I2S/TDM
→ Rockchip ASoC
→ ALSA PCM
→ PCM Samples + Timestamp

Control:
dx/dy
→ Linux UART
→ MCU/RTOS
→ Gimbal PWM
→ Pan/Tilt

Media:
Video Timestamp
+
Audio Timestamp
+
Status Timestamp
→ A/V Alignment
→ Encode / Mux
→ Network Stream
and/or
→ SD Card Recording

最后：
→ Performance / Latency / Stability Report
```

# 二、项目阶段规划

当前项目阶段规划：

* V0：产品定义与边界控制
* V1：RK3588 基础环境与工具链
* V2：Camera / V4L2 视频采集链路
* V2A：Audio / ALSA 音频采集与音视频时钟基线
* V3：足球分割模型迁移与 RKNN 推理
* V4：足球位置后处理与目标偏差计算
* V5：Linux → MCU/RTOS 通信协议
* V6：MCU/RTOS 云台电机控制
* V7：端到端视觉闭环追踪
* V8：音视频同步、编码/封装、网络回传和本地录像
* V9：性能、稳定性、SD 卡录制可靠性和产品化
* V10：README、用户手册、简历和面试包装

我现在只开始：

V2A：Audio / ALSA 音频采集与音视频时钟基线。

# 三、已经完成的工作

## V0 已完成

已经明确：

* 项目是边缘视觉云台追踪产品；
* 不是 AI demo；
* 第一版使用 RK3588；
* Linux 负责 Camera、NPU、Audio、网络、存储和高级策略；
* MCU/RTOS 负责云台实时控制；
* Linux → MCU 第一版采用 UART；
* 第一版优先完成最小闭环。

## V1 已完成

已经确认：

* RK3588 开发板稳定启动；
* Linux 登录正常；
* Bootloader 已记录；
* Kernel 已记录；
* RootFS 已记录；
* CPU/内存/存储/网络已记录；
* RKNPU 驱动正常；
* RKNN Runtime 正常；
* RKNPU 与 RKNN Runtime 版本关系已确认；
* C/C++ 本地或交叉编译部署链路已经跑通；
* ELF/ABI 已验证。

不要让我重新完整执行 V1。

## V2 已完成

Camera / V4L2 视频采集阶段已经完成。

当前已经实现：

* 正确识别 Camera capture node；
* V4L2 capability 已确认；
* pixel format / resolution / fps 已确认；
* V4L2 mmap streaming I/O 已实现；
* REQBUFS / QUERYBUF / mmap / QBUF / STREAMON / DQBUF / STREAMOFF 全链路正常；
* 自研 C/C++ 视频采集程序正常；
* frame sequence 正常；
* V4L2 timestamp 正常；
* timestamp interval 正常；
* 无异常帧丢失；
* 10 分钟稳定采集无异常；
* 当前 Camera 最高可稳定支持 4K@30fps；
* 已有视频帧时间戳 CSV/统计能力。

不要让我重新实现或重新验证完整 V2。

如果 V2A 需要视频时间戳作为对比，可以直接复用现有 V2 程序。

# 四、V2A 当前硬件

音频硬件已经明确：

```text
SoC:
RK3588

Audio Codec:
ES8388

Microphone:
开发板板载驻极体麦克风

Audio Framework:
Linux ASoC + ALSA

用户态目标接口：
alsa-lib / ALSA PCM

最终产品音频来源：
板载驻极体麦克风
```

请注意：

虽然已经知道 Audio Codec 是 ES8388，但以下内容仍不得猜测：

```text
ALSA card index：[待检测]
ALSA card name：[待检测]
PCM capture device：[待检测]
hw:CARD,DEV：[待检测]

ES8388 I2C bus/address：[待检测或从 DTS 确认]
RK3588 I2S controller：[待检测]
Machine driver：[待检测]

板载驻极体麦克风连接到：
LINPUT1 / LINPUT2 / RINPUT1 / RINPUT2 / 其他
：[必须从原理图、DTS、厂商文档或实际 routing 证明]

Mic Bias 配置：[待确认]

Capture mixer path：[待确认]
Capture gain：[待测试]

硬件支持的 sample format：[待检测]
硬件支持的 sample rates：[待检测]
硬件支持的 channel count：[待检测]
period size：[待测]
buffer size：[待测]
```

不能因为芯片是 ES8388，就自行假设麦克风连接到某一路输入。

# 五、V2A 一句话目标

V2A 的目标是：

确认 RK3588 → ASoC → ES8388 → 板载驻极体麦克风 → ALSA PCM 的真实音频采集链路，完成稳定的 PCM 音频采集程序，明确 format / sample rate / channel / period / buffer / mixer path，正确处理 XRUN，并建立能够与 V2 视频时间戳使用同一系统时间基线的音频时间戳与 sample clock 统计机制。

V2A 最终需要证明：

```text
Onboard Electret Mic
→ ES8388 Analog Input
→ ADC
→ I2S
→ RK3588 ASoC
→ ALSA Card
→ PCM Capture Device
→ alsa-lib
→ Stable PCM Samples
→ Audio Timestamp
→ Sample Counter
→ Drift / XRUN Statistics
```

# 六、V2A 不做什么

V2A 不做：

1. 不做 AAC 编码；
2. 不做 Opus 编码；
3. 不做 MP3 编码；
4. 不做 H.264/H.265 视频编码；
5. 不做 MP4/MKV mux；
6. 不做 GStreamer 产品 pipeline；
7. 不做 FFmpeg 产品 pipeline；
8. 不做 RTSP；
9. 不做音视频成品文件；
10. 不做真正的 A/V sync 算法；
11. 不做音频重采样补偿；
12. 不做 lip-sync 调优；
13. 不做网络音视频回传；
14. 不做 SD 卡录像；
15. 不做回声消除；
16. 不做 AGC 算法；
17. 不做降噪算法；
18. 不做波束形成；
19. 不做语音识别；
20. 不做 NPU 音频模型；
21. 不修改 ES8388 内核驱动；
22. 不直接写 ES8388 寄存器；
23. 不一开始修改 Device Tree；
24. 不一开始使用 `i2cset`；
25. 不一开始使用 `i2cdetect` 扫描整个总线；
26. 不盲目修改 mixer controls。

如果 ALSA card 和 PCM capture 已经存在，优先认为 BSP 的 Audio/ASoC bring-up 已完成。

只有获得明确证据证明 DTS、machine driver 或 codec routing 配置有问题时，才进入 BSP 层排查。

# 七、V2A 与 V8 的边界

V2A 只负责：

```text
Audio Capture
+
Audio Timestamp
+
Audio Sample Counter
+
Clock Baseline
+
XRUN / Stability
```

V8 才负责：

```text
Video Timestamp
+
Audio Timestamp
→ A/V Synchronization
→ Encode
→ Mux
→ Stream / Record
```

因此 V2A 要为 V8 提供“可同步的数据”，但不能提前把 V2A 做成完整多媒体系统。

# 八、资料搜索要求

遇到技术问题时必须优先网页搜索，资料优先级：

1. 当前开发板厂商官方 Wiki；
2. 当前开发板官方原理图；
3. 当前开发板 BSP / DTS；
4. Linux Kernel ASoC 官方文档；
5. Linux Kernel ALSA PCM timestamp 官方文档；
6. ALSA Project 官方 alsa-lib 文档；
7. ALSA Project alsa-utils 文档；
8. Linux ES8388 / ES8328 codec binding 和 driver；
9. ES8388 Datasheet；
10. Rockchip 官方 Audio/I2S 文档；
11. 高质量社区资料仅作为辅助。

必须注意：

ES8388 codec 层与具体开发板 machine routing 是两件事。

不能因为网络上另一块 RK3588 + ES8388 开发板使用：

```text
Capture MIC Path
Capture Volume
```

就假定当前开发板具有相同 control。

必须先查看实际：

```bash
amixer controls
amixer contents
```

再决定 mixer 设置。

# 九、V2A 必须产出的交付物

建议新增目录：

```text
VisionArm-BallTrack/
├── README.md
├── docs/
│   └── v2a_audio_alsa/
│       ├── README.md
│       ├── audio_device_inventory.md
│       ├── es8388_capture_path.md
│       ├── alsa_capture_design.md
│       ├── audio_clock_baseline.md
│       ├── performance_report.md
│       ├── troubleshooting.md
│       └── v2a_acceptance.md
│
├── src/
│   └── linux_app/
│       └── audio/
│           ├── CMakeLists.txt
│           ├── alsa_capture.h
│           ├── alsa_capture.cpp
│           ├── wav_writer.h
│           ├── wav_writer.cpp
│           └── main.cpp
│
├── tools/
│   └── v2a_audio/
│       ├── collect_audio_info.sh
│       ├── record_smoke_test.sh
│       └── analyze_wav.py
│
├── build/
│   └── v2a_audio/
│
├── logs/
│   └── v2a_audio/
│       ├── cards.txt
│       ├── pcm.txt
│       ├── arecord_list.txt
│       ├── arecord_pcms.txt
│       ├── mixer_controls.txt
│       ├── mixer_contents.txt
│       ├── asoc_dmesg.txt
│       ├── hw_params.txt
│       ├── alsa_state_before.state
│       ├── smoke_test.log
│       ├── stability_test.log
│       ├── audio_frames.csv
│       └── av_clock_baseline.csv
│
└── reports/
    └── audio/
        ├── microphone_test.wav
        └── waveform_stats.txt
```

## P0 必须完成

1. 正确识别 ALSA sound card；
2. 正确识别 PCM capture device；
3. 证明该 PCM 对应 ES8388；
4. 明确板载麦克风 capture path；
5. 保存 mixer controls；
6. 修改 mixer 前保存 ALSA state；
7. 使用 `arecord` 成功录制；
8. WAV 中能够确认真实麦克风声音；
9. 明确真实硬件 format；
10. 明确真实 sample rate；
11. 明确真实 channel count；
12. 明确实际 period size；
13. 明确实际 buffer size；
14. 自研 alsa-lib C/C++ capture 程序；
15. PCM 连续采集稳定；
16. XRUN 可检测；
17. XRUN 恢复逻辑存在；
18. 音频 sample counter 可统计；
19. ALSA timestamp 可记录；
20. 系统 monotonic timestamp 可记录；
21. 10 分钟稳定测试；
22. sample-clock drift 有统计；
23. 音频波形 peak/RMS/clipping 等基本指标；
24. V2A 文档完成；
25. README 更新；
26. Git 提交。

## P1 推荐完成

1. 同时运行 V2 Camera 和 V2A Audio 10 分钟；
2. 两者都记录 monotonic 时间；
3. 验证音视频采集能够共存；
4. 记录共同采集期间 CPU 使用率；
5. 记录 audio sample clock 与 system clock drift；
6. 对比 16 kHz / 44.1 kHz / 48 kHz 等实际支持率；
7. 对 period/buffer 参数进行小范围对比；
8. 记录 microphone noise floor；
9. 记录 clipping；
10. 记录 gain 设置。

## P2 可延期

1. AAC；
2. Opus；
3. MP3；
4. Audio resampler；
5. AV sync correction；
6. GStreamer；
7. FFmpeg mux；
8. RTSP；
9. SD 卡录像；
10. AEC；
11. AGC；
12. Noise Suppression；
13. Speech Recognition。

# 十、你第一次回复必须严格输出以下 13 部分

## 1. V2A 一句话目标

说明这个阶段到底解决什么工程问题。

必须说明为什么：

* 不能直接把麦克风交给 FFmpeg/GStreamer；
* 不能直接开始 MP4 录制；
* 需要先独立证明 ALSA capture 和 timestamp。

## 2. V2A 不做什么

严格写清阶段边界。

必须明确：

音频编码、音视频同步、mux、网络回传、本地视频录像属于 V8。

SD 卡长期可靠性属于 V9。

## 3. V2A 最小可交付物

按照：

* P0；
* P1；
* P2；

分别列出：

* 文档；
* 日志；
* 源码；
* WAV；
* CSV；
* 测试数据；
* Git commit。

## 4. 推荐目录结构

给出本阶段完整目录树。

说明每个文件的用途。

不要重新创建 V0/V1/V2 的工程。

## 5. 当前阶段 Markdown 文档模板

直接提供以下文件的可复制 Markdown 模板。

### `audio_device_inventory.md`

必须包含：

* 开发板信息；
* Kernel；
* ALSA version；
* codec；
* ASoC card；
* card index；
* card name；
* PCM device；
* capture subdevice；
* `/proc/asound/cards`；
* `/proc/asound/pcm`；
* `arecord -l`；
* `arecord -L`；
* codec driver；
* I2S controller；
* machine driver；
* DTS 节点；
* ES8388 I2C 地址；
* 麦克风类型；
* 最终 capture PCM；
* 选择依据；
* 未知项。

### `es8388_capture_path.md`

必须包含：

```text
Electret Microphone
→ Mic Bias
→ ES8388 Analog Input
→ PGA / Mixer
→ ADC
→ I2S DAI
→ RK3588 I2S RX
→ DMA
→ ASoC PCM
→ ALSA
```

并记录：

* 麦克风连接引脚；
* 证据来源；
* Mic Bias；
* Capture source；
* Capture switch；
* Capture volume；
* ADC controls；
* 实际 `amixer` control 名称；
* 原始 ALSA state；
* 修改后的 state；
* 修改原因；
* 回滚方法。

### `alsa_capture_design.md`

至少包含：

* 目标；
* 为什么使用 alsa-lib；
* 为什么正式能力验证优先使用 `hw:CARD,DEV`；
* `hw:` 和 `plughw:` 区别；
* PCM open；
* access mode；
* format；
* rate；
* channel；
* period；
* buffer；
* hw_params；
* sw_params；
* timestamp mode；
* timestamp type；
* poll/readi；
* sample counter；
* XRUN；
* EPIPE；
* ESTRPIPE；
* WAV 保存；
* CSV；
* signal handling；
* 资源释放；
* 后续 V8 接口。

### `audio_clock_baseline.md`

必须包含：

* V2 视频时间戳现状；
* V4L2 timestamp clock；
* ALSA timestamp type；
* `CLOCK_MONOTONIC`；
* `CLOCK_MONOTONIC_RAW`；
* 为什么不能混用；
* audio trigger timestamp；
* audio current/status timestamp；
* application dequeue/read timestamp；
* cumulative samples；
* nominal audio time；
* clock drift；
* drift ppm；
* V2A 只建立时间基线；
* V8 才做真正 A/V sync；
* 当前结论。

### `performance_report.md`

必须包含：

* 测试环境；
* PCM device；
* format；
* sample rate；
* channels；
* bits/sample；
* period frames；
* buffer frames；
* period duration；
* buffer duration；
* 测试时长；
* total frames；
* total samples；
* nominal audio duration；
* monotonic elapsed；
* drift；
* drift ppm；
* XRUN count；
* recover count；
* short read；
* read errors；
* peak；
* RMS；
* DC offset；
* clipping count；
* CPU usage；
* memory usage；
* 原始日志；
* 结论。

### `troubleshooting.md`

至少包含：

* 找不到 sound card；
* 找不到 capture PCM；
* card index 变化；
* `arecord: device busy`；
* `Invalid argument`；
* format 不支持；
* rate 不支持；
* channel 不支持；
* `snd_pcm_hw_params` 失败；
* 没有声音；
* 全零数据；
* 只有噪声；
* 音量过小；
* 严重削波；
* 左右声道只有一路；
* Mic Bias 问题；
* Capture Path 错误；
* mixer control 名称不一致；
* XRUN/EPIPE；
* suspend/ESTRPIPE；
* readi short read；
* timestamp 不可用；
* timestamp 类型不匹配；
* CPU 负载导致 XRUN；
* period 太小；
* buffer 太小；
* 48k 配置失败；
* plughw 能录而 hw 不能录；
* PulseAudio/PipeWire 占用；
* ALSA state 被桌面环境修改；
* V2 Camera 与 Audio 同时运行出现异常。

### `v2a_acceptance.md`

至少包含：

| 验收项 | 证据 | 状态 | 备注 |
| --- | -- | -- | -- |

状态只能使用：

```text
PASS
WARN
FAIL
BLOCKED
```

并包含：

* 是否允许进入 V3；
* 是否具备进入 V8 音视频同步的音频基础；
* 阻塞项；
* 最终结论。

## 6. 技术链路图或文字流程

必须给出至少以下三个图。

### ASoC 硬件链路

```text
Onboard Electret Microphone
        ↓
      Mic Bias
        ↓
ES8388 Analog Input
        ↓
PGA / Analog Mixer
        ↓
       ADC
        ↓
ES8388 Digital Audio Interface
        ↓ I2S
RK3588 I2S Controller
        ↓
       DMA
        ↓
 Linux ASoC
        ↓
 ALSA PCM Device
        ↓
   alsa-lib
        ↓
Application
```

### ALSA PCM 数据路径

```text
snd_pcm_open()
→ hw_params_any
→ access
→ format
→ channels
→ sample rate
→ period size
→ buffer size
→ hw_params
→ sw_params
→ timestamp enable
→ prepare
→ poll / snd_pcm_readi
→ PCM frames
→ timestamp + sample counter
→ CSV / WAV
→ repeat
→ drain/drop
→ close
```

### 后续音视频同步接口

```text
V2 Video:

frame_id
v4l2_sequence
video_timestamp
dq_monotonic_timestamp

                +
                |
                v

V2A Audio:

audio_chunk_id
cumulative_frames
frames_read
alsa_timestamp
audio_monotonic_timestamp
nominal_sample_time

                ↓

V8:
Common Timeline
→ Drift Estimation
→ Sync Policy
→ Encode
→ Mux
```

必须强调：

V2A 不做 mux。

## 7. 具体执行步骤

必须按下面顺序推进。

### Step A：只读 ALSA/ASoC 盘点

禁止先修改 mixer。

先确认：

* sound card；
* PCM capture；
* ES8388 driver；
* I2S；
* machine card；
* DTS routing；
* mixer controls。

### Step B：保存原始 ALSA State

在修改任何 mixer control 以前保存完整 state。

必须提供恢复方法。

### Step C：确认板载驻极体麦克风物理路径

证据优先级：

```text
原理图
→ DTS
→ 官方 BSP 文档
→ ASoC routing
→ mixer controls
→ 实际录音实验
```

不能反过来只靠试错猜。

### Step D：枚举硬件 PCM 能力

优先直接测试：

```text
hw:CARD,DEV
```

而不是一开始使用 `default` 或 `plughw`。

目的：

防止 alsa-lib plugin 自动完成：

* resampling；
* format conversion；
* channel conversion；

导致我误认为硬件原生支持某参数。

### Step E：arecord 最小录音实验

先使用设备原生支持的：

* format；
* rate；
* channels。

录制 10～30 秒 WAV。

验证：

* 文件有效；
* 人说话/拍手能在波形中看出；
* 可以复制到 PC 播放；
* 不全零；
* 不严重 clipping；
* 声道正确。

不要为了测试录音链路而同时调试开发板扬声器播放链路。

### Step F：确定 MVP Audio Format

评估实际硬件能力后决定。

产品候选优先考虑：

```text
48 kHz
S16_LE
mono
```

但这只是候选，不得强制。

如果 ES8388/ASoC 的 `hw:` PCM 实际要求 stereo，则优先：

```text
48 kHz
S16_LE
2 channels
```

保存原生 stereo PCM，后续 V8 如有需要再 downmix。

不要在 V2A 为了获得 mono 而隐藏一次软件 channel conversion。

### Step G：设计 period / buffer

不能拍脑袋选择。

至少记录：

```text
sample_rate
period_frames
period_time_ms
periods
buffer_frames
buffer_time_ms
```

先使用稳定的中等参数。

不要一开始追求极低 latency。

### Step H：编写自研 alsa-lib C++ Capture

必须完整实现，不允许只给伪代码。

### Step I：Timestamp 和 Sample Clock

开启 ALSA timestamp。

优先尝试使用与 V2 V4L2 timestamp 一致的 clock domain。

如果 V2 已确认使用：

```text
CLOCK_MONOTONIC
```

则 ALSA 优先尝试：

```text
SND_PCM_TSTAMP_TYPE_MONOTONIC
```

如果硬件或 alsa-lib 不支持，必须记录真实行为。

不能静默 fallback。

每个 audio chunk 至少保存：

```text
chunk_index
frames_read
cumulative_frames
alsa_status_timestamp
alsa_trigger_timestamp
app_monotonic_before_read
app_monotonic_after_read
avail
delay
xrun_count
```

同时计算：

```text
nominal_audio_time =
    cumulative_frames / actual_sample_rate
```

然后与系统 monotonic elapsed 对比。

### Step J：短时测试

至少 30～60 秒。

统计：

* frames；
* XRUN；
* read errors；
* timestamps；
* sample-clock drift；
* peak/RMS/clipping。

### Step K：10 分钟稳定性测试

要求：

* 不保存巨量 WAV 也可以；
* 可以直接丢弃 PCM payload；
* 但必须完整运行 ALSA capture loop；
* 必须保留 CSV 和统计；
* 不能通过 sleep 模拟采集。

### Step L：Video + Audio 共存实验

这是 V2A 的推荐实验，但仍然不是 A/V sync。

同时运行：

* 已经通过 V2 验收的 Camera capture；
* 当前 ALSA capture。

持续建议 10 分钟。

验证：

* Camera 仍无异常；
* Audio 无 XRUN；
* CPU/内存合理；
* 两边都存在可比较的 monotonic timestamp。

不要将两者 mux。

### Step M：报告与 Git 提交

原始日志和人工分析必须分开。

每个步骤必须说明：

1. 在 RK3588 还是开发 PC 执行；
2. 目的；
3. 命令；
4. 正常结果；
5. 异常判断；
6. 日志保存位置；
7. 是否阻塞下一步。

# 十一、推荐命令和代码要求

## 1. 创建目录

提供：

```bash
mkdir -p docs/v2a_audio_alsa
mkdir -p src/linux_app/audio
mkdir -p tools/v2a_audio
mkdir -p build/v2a_audio
mkdir -p logs/v2a_audio
mkdir -p reports/audio
```

## 2. ALSA 基础盘点

至少给出：

```bash
cat /proc/asound/cards
cat /proc/asound/pcm
cat /proc/asound/devices

arecord --version
arecord -l
arecord -L

aplay -l
aplay -L
```

说明：

`aplay` 在这里主要辅助盘点 sound card，不代表 V2A 必须调试 Playback。

## 3. ASoC / Kernel 信息

至少包含：

```bash
dmesg | grep -iE 'es8388|es8328|asoc|alsa|audio|sound|i2s'

lsmod | grep -iE 'snd|es83|rockchip'

find /sys/bus/i2c/devices \
  -maxdepth 2 \
  -name name \
  -print \
  -exec cat {} \; 2>/dev/null
```

如果 debugfs 已经挂载，可查看：

```text
/sys/kernel/debug/asoc/
```

但不能为了本阶段一开始就修改系统配置。

如果 `dtc` 已安装，可以把当前 live device tree 导出为只读分析材料，例如：

```bash
dtc -I fs -O dts /proc/device-tree \
  > logs/v2a_audio/live_device_tree.dts
```

如果 `dtc` 不存在，不允许只为了这一项马上安装软件。

## 4. Mixer 只读盘点

确定 `<CARD>` 后必须提供：

```bash
amixer -c <CARD> scontrols
amixer -c <CARD> controls
amixer -c <CARD> contents
```

分别保存到日志。

禁止在看到输出前写类似：

```bash
amixer cset ...
```

## 5. 保存原始 ALSA State

必须给出适用于当前系统的：

```bash
alsactl -f logs/v2a_audio/alsa_state_before.state \
  store <CARD>
```

并给出对应恢复方式。

修改 mixer 前必须执行。

## 6. PCM Hardware Parameters

必须首先通过实际设备检测。

例如根据当前 alsa-utils 支持情况使用：

```bash
arecord \
  -D hw:<CARD>,<DEVICE> \
  --dump-hw-params \
  -t raw \
  -d 1 \
  /dev/null
```

如果命令因当前 alsa-utils 语法不同失败，应先：

```bash
arecord --help
```

不得直接改用 `plughw` 来掩盖问题。

## 7. arecord Smoke Test

在实际能力确认后提供类似：

```bash
arecord \
  -D hw:<CARD>,<DEVICE> \
  -f <FORMAT> \
  -r <RATE> \
  -c <CHANNELS> \
  -d 20 \
  reports/audio/microphone_test.wav \
  2>&1 | tee logs/v2a_audio/smoke_test.log
```

参数必须来自检测结果。

不能自行填写。

## 8. WAV 基础验证

至少包含：

```bash
file reports/audio/microphone_test.wav
stat reports/audio/microphone_test.wav
sha256sum reports/audio/microphone_test.wav
```

如果系统有 `soxi`：

```bash
soxi reports/audio/microphone_test.wav
```

如果系统有 `ffprobe`：

```bash
ffprobe -hide_banner reports/audio/microphone_test.wav
```

但不得为了 V2A 强制安装 FFmpeg。

最重要的是：

把 WAV 复制到 PC 后实际试听，并进行波形统计。

## 9. Audio 信息收集脚本

直接提供完整：

```text
tools/v2a_audio/collect_audio_info.sh
```

要求：

1. `#!/usr/bin/env bash`；
2. 不修改系统；
3. 不修改 mixer；
4. 不安装包；
5. 不执行 i2cset；
6. 不自动扫描危险 I2C 地址；
7. 单个命令失败继续执行；
8. 缺失工具标记 SKIPPED；
9. 每项有标题；
10. 记录时间；
11. 保存到 `logs/v2a_audio/`；
12. 可传入 card index；
13. 可传入 PCM device；
14. 默认只执行只读操作。

## 10. 自研 ALSA Capture 程序

必须直接提供完整可编译 C++ 程序：

```text
src/linux_app/audio/
├── CMakeLists.txt
├── alsa_capture.h
├── alsa_capture.cpp
├── wav_writer.h
├── wav_writer.cpp
└── main.cpp
```

不得只给伪代码。

主要依赖：

```text
alsa-lib
```

不得依赖：

* OpenCV；
* FFmpeg；
* GStreamer；
* PulseAudio；
* PipeWire。

CMake 至少：

```text
C++17
-Wall
-Wextra
-Wpedantic
```

使用：

```text
pkg-config alsa
```

或：

```text
-lasound
```

编译前先检测开发环境，不要直接安装软件。

程序至少支持：

```text
--device <hw:CARD,DEV>
--rate <Hz>
--channels <count>
--format <S16_LE/...>
--period-frames <count>
--buffer-frames <count>
--duration <seconds>
--output <wav/raw>
--csv <path>
--no-save
--timestamp-type <monotonic/monotonic-raw>
--stats-interval <seconds>
```

程序必须实现：

### PCM 初始化

```text
snd_pcm_open
snd_pcm_hw_params_any
snd_pcm_hw_params_set_access
snd_pcm_hw_params_set_format
snd_pcm_hw_params_set_channels
snd_pcm_hw_params_set_rate_near
snd_pcm_hw_params_set_period_size_near
snd_pcm_hw_params_set_buffer_size_near
snd_pcm_hw_params
```

必须打印：

```text
requested_rate
actual_rate
requested_channels
actual_channels
requested_format
actual_format
requested_period_frames
actual_period_frames
requested_buffer_frames
actual_buffer_frames
```

如果 ALSA 自动选择 nearest value，必须把 requested 和 actual 分开记录。

### Software Params

必须研究并合理配置：

```text
snd_pcm_sw_params_current
snd_pcm_sw_params_set_avail_min
snd_pcm_sw_params_set_tstamp_mode
snd_pcm_sw_params_set_tstamp_type
snd_pcm_sw_params
```

不要为了“低延迟”随意设置极端参数。

### Capture

优先第一版实现：

```text
SND_PCM_ACCESS_RW_INTERLEAVED
+
snd_pcm_readi()
```

这是可以接受的。

V2A 不强制 mmap PCM。

以后如有低延迟需要再扩展。

### Timestamp

程序至少需要获取：

```text
CLOCK_MONOTONIC application timestamps

ALSA status timestamp

ALSA trigger timestamp
```

根据当前 alsa-lib 版本合理使用：

```text
snd_pcm_status
snd_pcm_status_get_htstamp
snd_pcm_status_get_trigger_htstamp
```

如果有更可靠的 ALSA API，例如：

```text
snd_pcm_htimestamp()
```

可以同时记录。

必须解释这些 timestamp 各自代表什么。

不能把应用 `readi()` 返回时间直接声称为 ADC 采样时间。

### PCM Frame Accounting

必须正确理解：

```text
1 PCM frame =
所有 channel 在一个 sample instant 的数据
```

例如：

```text
2 channel
S16_LE

1 frame = 4 bytes
```

但代码必须动态计算，而不是写死。

程序至少统计：

```text
total_frames
total_samples
total_bytes
cumulative_frames
nominal_audio_duration
```

### XRUN

必须处理：

```text
-EPIPE
```

记录：

```text
xrun_count
xrun_timestamp
recovery_count
```

可以使用：

```text
snd_pcm_recover()
```

或明确恢复流程。

不能发生 XRUN 后悄悄继续而不记录。

### Suspend

如果遇到：

```text
-ESTRPIPE
```

必须有明确处理逻辑或至少明确错误报告。

### Error Handling

至少区分：

```text
EPIPE
ESTRPIPE
EAGAIN
其他错误
```

### WAV

第一版只需要支持一个明确的 PCM WAV 子集即可，例如：

```text
S16_LE PCM
```

如果当前硬件最终选择其他格式：

* 可以先输出 raw PCM；
* 或完善 WAV writer。

不能写错误 WAV header。

程序退出时必须正确回填：

```text
RIFF size
data size
```

### Signal Metrics

如果实际 MVP 使用 `S16_LE`，至少实时统计：

```text
sample_min
sample_max
peak
RMS
DC offset
clipping_samples
```

如果 stereo，应支持：

```text
left peak/RMS
right peak/RMS
```

这样可以判断板载麦克风究竟在哪个 channel。

不得仅通过“听起来有声音”确定声道。

### Signal Handling

支持：

```text
SIGINT
SIGTERM
```

能够安全停止：

* capture；
* WAV；
* CSV；
* PCM；
* 文件。

## 11. CSV 设计

每个 audio chunk 至少保存：

```text
chunk_index
frames_requested
frames_read
cumulative_frames
bytes_read

app_mono_before_ns
app_mono_after_ns

alsa_timestamp_sec
alsa_timestamp_nsec

trigger_timestamp_sec
trigger_timestamp_nsec

avail_frames
delay_frames

xrun_count
recovery_count

chunk_peak
chunk_rms
```

如果某 ALSA timestamp API 不可用：

必须：

```text
NA
```

不能伪造。

## 12. Drift 统计

必须计算：

```text
nominal_audio_duration_s =
    total_frames / actual_rate
```

以及：

```text
monotonic_elapsed_s
```

计算：

```text
drift_s =
    nominal_audio_duration_s
    - monotonic_elapsed_s
```

以及：

```text
drift_ppm =
    drift_s
    / monotonic_elapsed_s
    * 1,000,000
```

必须说明：

这个结果是当前 Audio Sample Clock 与系统时间基线之间的工程测量值。

不能仅凭一次短测就得出长期同步结论。

## 13. 与 V2 Camera 的时间基线

读取我已经完成的 V2 设计。

首先确认视频端实际使用：

```text
V4L2 timestamp clock type
```

如果 V2 已确认是：

```text
V4L2_BUF_FLAG_TIMESTAMP_MONOTONIC
```

则本阶段应优先让 ALSA 使用：

```text
SND_PCM_TSTAMP_TYPE_MONOTONIC
```

建立共同时间基线。

如果视频实际不是 MONOTONIC：

不能强行假设。

必须先说明两边 clock domain，再设计后续 V8 的 timestamp normalization。

# 十二、最小验证实验

必须定义下面的实验。

## 实验 1：ALSA Card 身份

证明：

```text
ALSA card
→ ES8388
→ 正确 PCM capture device
```

不能仅靠 card index。

因为 card index 可能重启后变化。

尽量记录 card name 和稳定标识。

## 实验 2：板载麦克风路径

通过：

```text
原理图/DTS
+
mixer controls
+
录音实验
```

证明板载 electret microphone 的 capture path。

## 实验 3：arecord 录音

录制至少 20 秒。

测试动作建议：

```text
0-5s：安静
5-10s：说话
10-15s：拍手
15-20s：说话
```

用于辨识真实信号。

## 实验 4：声道确认

如果硬件暴露 2-channel：

分别统计 L/R：

```text
RMS
peak
```

判断：

* 两路都有信号；
* 只有左路；
* 只有右路；
* 是否存在复制声道。

不能凭感觉判断。

## 实验 5：自研 C++ Capture

必须绕开：

```text
arecord
```

由自己的 alsa-lib 程序完成采集。

## 实验 6：30～60 秒统计

要求：

* 0 XRUN 为目标；
* timestamp 正常；
* frames 连续；
* sample accounting 正确。

## 实验 7：10 分钟稳定性

至少：

```text
10 minutes
```

要求记录：

* XRUN count；
* recovery count；
* total frames；
* actual elapsed；
* drift；
* drift ppm；
* CPU；
* memory。

不要规定一个未经测量依据的 ppm PASS 阈值。

第一轮先建立 baseline。

## 实验 8：V2 + V2A 同时运行

Camera：

复用已完成的 V2 程序。

Audio：

运行 V2A 程序。

建议：

```text
10 minutes
```

验证：

```text
Video:
无异常掉帧
timestamp 正常

Audio:
无 XRUN
timestamp 正常

System:
无 OOM
无明显异常负载
```

这仍然不是 A/V synchronization。

## 实验 9：共同 Clock Baseline

从两份 CSV 中确认：

```text
Video monotonic timestamps
Audio monotonic timestamps
```

处于可以比较的同一时间域。

V2A 到此即可。

不要在这里做 mux。

# 十三、常见坑和严格排查顺序

必须详细覆盖以下问题。

## 常见坑

1. 把 `card 0` 写死；
2. 把 `default` 当作真实硬件；
3. 一开始使用 `plughw`；
4. plughw 自动 resample 后误认为硬件支持；
5. 没有区分 Playback 和 Capture PCM；
6. ES8388 card 存在但麦克风 routing 错误；
7. Mic Bias 未开启；
8. Capture source 选错；
9. Capture switch muted；
10. Capture volume 太低；
11. gain 太高导致 clipping；
12. 只改 mixer 不保存原状态；
13. 直接复制其他 RK3588 板的 `amixer cset`；
14. 使用错误 card index；
15. 麦克风只有一个 channel 有效；
16. stereo interleaved 解析错误；
17. frame 和 sample 概念混淆；
18. sample rate 请求值和 actual 值不一致；
19. period size 请求值和 actual 值不一致；
20. buffer size 设置不合理；
21. period 太小导致 XRUN；
22. 忽略 `snd_pcm_readi()` short read；
23. 忽略 EPIPE；
24. XRUN 恢复后不计数；
25. timestamp 未开启；
26. ALSA timestamp 和 application timestamp 混淆；
27. REALTIME 和 MONOTONIC 混用；
28. MONOTONIC 和 MONOTONIC_RAW 混用但没有记录；
29. 把 readi 返回时刻称为“采样时间”；
30. 不记录 cumulative frames；
31. 没有 sample-clock drift；
32. WAV header 错误；
33. 10 分钟测试直接产生超大日志；
34. PulseAudio/PipeWire 抢占 PCM；
35. 音频 PCM 被其他进程占用；
36. root 用户测试正常、普通用户权限失败；
37. 一遇到无声就改 DTS；
38. 一遇到无声就用 i2cset 写 ES8388；
39. 一遇到无声就换 kernel；
40. 一开始直接接 FFmpeg/GStreamer；
41. V2A 没完成就开始 MP4 mux；
42. 同时运行 Camera 后出现 Audio XRUN，却误判为 Codec 问题。

## 排查顺序必须严格是

```text
ASoC card 是否注册
↓
ALSA card 身份
↓
PCM capture 是否存在
↓
ES8388 driver 是否绑定
↓
Machine driver / DTS routing
↓
板载麦克风物理连接
↓
Mixer read-only inventory
↓
保存 ALSA state
↓
Capture source / Mic Bias / Gain
↓
arecord + hw PCM
↓
format/rate/channels
↓
PCM signal validity
↓
自研 alsa-lib capture
↓
period / buffer
↓
XRUN
↓
timestamp
↓
sample clock drift
↓
Camera + Audio coexistence
```

不能一遇到问题就：

* 重刷系统；
* 修改 kernel；
* 修改 Device Tree；
* 写 ES8388 registers。

# 十四、V2A 完成前自查清单

给我完整可复制 Markdown checklist。

至少包含：

## ALSA 身份

* [ ] `/proc/asound/cards` 已保存
* [ ] `/proc/asound/pcm` 已保存
* [ ] `arecord -l` 已保存
* [ ] `arecord -L` 已保存
* [ ] ES8388 card 已确认
* [ ] Capture PCM 已确认
* [ ] 没有依赖 card index 猜测设备

## Hardware Path

* [ ] 板载驻极体麦克风路径已确认
* [ ] Mic Bias 状态已确认
* [ ] ES8388 input 已确认
* [ ] I2S path 已确认
* [ ] ASoC routing 已记录

## Mixer

* [ ] `amixer controls` 已保存
* [ ] `amixer contents` 已保存
* [ ] 原始 ALSA state 已保存
* [ ] mixer 修改有理由
* [ ] mixer 修改可恢复

## PCM Parameters

* [ ] hardware format 已确认
* [ ] hardware sample rate 已确认
* [ ] hardware channels 已确认
* [ ] actual period 已记录
* [ ] actual buffer 已记录
* [ ] MVP audio format 已确定

## Signal

* [ ] arecord WAV 有真实声音
* [ ] WAV 非全零
* [ ] peak 正常
* [ ] RMS 正常
* [ ] clipping 已统计
* [ ] channel 分布已确认

## C/C++

* [ ] 自研 alsa-lib 程序可以运行
* [ ] 参数可以命令行配置
* [ ] 实际 hw params 会打印
* [ ] frame/sample/byte accounting 正确
* [ ] WAV/raw 输出正确
* [ ] Ctrl+C 安全退出

## Stability

* [ ] 30～60 秒测试完成
* [ ] 10 分钟测试完成
* [ ] XRUN 已统计
* [ ] recover 已统计
* [ ] read errors 已统计
* [ ] CPU 已记录
* [ ] memory 已记录

## Timestamp

* [ ] ALSA timestamp mode 已确认
* [ ] ALSA timestamp type 已确认
* [ ] trigger timestamp 已记录
* [ ] current/status timestamp 已记录
* [ ] application MONOTONIC timestamp 已记录
* [ ] cumulative frames 已记录
* [ ] nominal audio time 已计算
* [ ] drift 已计算
* [ ] drift ppm 已计算

## A/V 基线

* [ ] V2 视频 clock type 已确认
* [ ] Audio 和 Video clock domain 已比较
* [ ] Camera + Audio 并行采集验证完成
* [ ] 没有在 V2A 提前做 mux

## 文档

* [ ] audio_device_inventory.md 完成
* [ ] es8388_capture_path.md 完成
* [ ] alsa_capture_design.md 完成
* [ ] audio_clock_baseline.md 完成
* [ ] performance_report.md 完成
* [ ] troubleshooting.md 完成
* [ ] v2a_acceptance.md 完成
* [ ] README 已更新
* [ ] Git 已提交

# 十五、V2A 延期时降级方案

## P0 不能砍

1. ALSA card 身份；
2. ES8388 身份；
3. PCM capture device；
4. 板载麦克风真实 capture path；
5. mixer state；
6. arecord 成功；
7. 有真实声音 WAV；
8. format；
9. rate；
10. channels；
11. 自研 alsa-lib capture；
12. XRUN 统计；
13. cumulative frames；
14. monotonic timestamp；
15. ALSA timestamp；
16. 10 分钟稳定测试；
17. drift baseline；
18. 文档；
19. Git。

## 可以降级

1. 只测试一个最终 MVP sample rate；
2. 只使用 `RW_INTERLEAVED`；
3. 不做 ALSA mmap；
4. 暂时不做复杂 poll；
5. 不做长期 WAV 保存；
6. 不做多个 period 参数 benchmark；
7. Camera + Audio 联合测试可从 10 分钟降到 5 分钟，但必须标记 WARN；
8. 暂时不制定严格 drift ppm 指标。

## 可以完全延期

1. AAC；
2. Opus；
3. Resampler；
4. A/V synchronization correction；
5. MP4；
6. MKV；
7. RTSP；
8. Network streaming；
9. SD recording；
10. AGC；
11. AEC；
12. Noise suppression。

# 十六、面试可讲点

V2A 完成后，我应该能够解释：

1. Linux ASoC 与 ALSA 的关系；
2. Codec driver、CPU DAI、machine driver 的关系；
3. ES8388 在系统中的作用；
4. 驻极体麦克风为什么需要模拟前端/Mic Bias；
5. Audio Codec ADC 与 I2S 的关系；
6. `/proc/asound/cards`；
7. PCM device；
8. ALSA `hw:` 和 `plughw:` 的区别；
9. 为什么底层验证使用 `hw:`；
10. PCM frame 与 sample 的区别；
11. sample format；
12. sample rate；
13. channel；
14. period；
15. buffer；
16. alsa-lib hw_params；
17. alsa-lib sw_params；
18. `snd_pcm_readi()`；
19. XRUN/EPIPE；
20. XRUN recovery；
21. ALSA timestamp；
22. trigger timestamp；
23. MONOTONIC 和 REALTIME 区别；
24. Audio sample clock 与 system clock 的区别；
25. drift ppm；
26. 为什么音视频同步不能只靠线程同时启动；
27. 为什么 V2A 要给 V8 留 timestamp；
28. 如何验证板载麦克风到底在哪个 channel；
29. 如何避免误修改 ES8388 mixer；
30. 如何验证 Camera 与 Audio 能否同时稳定运行。

# 十七、V2A → 后续阶段准入条件

V2A 完成后才允许将 Audio 模块视为可交付输入源。

最低条件：

* [ ] ES8388 ALSA card 已明确
* [ ] capture PCM 已明确
* [ ] 板载驻极体麦克风路径已明确
* [ ] mixer 配置已明确
* [ ] mixer 配置有备份和回滚方式
* [ ] 原生硬件 format/rate/channels 已明确
* [ ] arecord 录音成功
* [ ] WAV 有真实有效声音
* [ ] 自研 C/C++ ALSA capture 正常
* [ ] 10 分钟连续采集稳定
* [ ] XRUN 有统计
* [ ] timestamp 有统计
* [ ] cumulative sample/frame counter 正常
* [ ] sample clock drift baseline 已得到
* [ ] V2 Camera clock type 已确认
* [ ] Audio/Video 已具备共同时间基线或已明确两者差异
* [ ] Camera + Audio 并行运行无阻塞问题
* [ ] V2A 测试报告完成
* [ ] README 更新
* [ ] Git 提交完成

如果以下任一情况存在，V2A 必须标记 BLOCKED：

1. ES8388 card 无法确认；
2. 没有 PCM capture device；
3. 板载麦克风 routing 完全不明确；
4. 只能通过 plughw 录音、真实 hw PCM 能力仍未知；
5. 录音数据全零；
6. 只能得到不可解释的噪声；
7. 持续发生 XRUN 且原因未确定；
8. 10 分钟测试无法通过；
9. timestamp 完全没有建立；
10. 不能统计 cumulative PCM frames；
11. 原始 mixer 状态未保存却进行了大量修改；
12. Audio 与 Camera 同时运行存在严重资源冲突且原因未确定。

V2A 完成后：

可以继续 V3 RKNN 推理阶段。

V8 开始前必须保留：

```text
AudioFrame / AudioChunk {
    data
    frames
    channels
    sample_rate
    format
    cumulative_frames
    alsa_timestamp
    monotonic_timestamp
}
```

作为后续 Audio/Video pipeline 的正式接口。

# 十八、后续与你的协作方式

后续对话必须遵守：

1. 先只读检测，后修改；
2. 第一轮不要直接给 `amixer cset`；
3. 我贴 `arecord -l` 后再判断 card；
4. 我贴 `amixer contents` 后再判断 mixer；
5. 不猜 Mic Bias control 名；
6. 不猜 Capture Path control 名；
7. 不猜 card index；
8. 不猜 PCM device；
9. 不猜 sample rate；
10. 不猜 channels；
11. 不猜 period/buffer；
12. 每个判断都引用真实输出；
13. 每项结果使用 PASS/WARN/FAIL/BLOCKED；
14. 未确认内容标记 UNKNOWN；
15. 修改 mixer 前必须备份；
16. 修改 DTS 前必须证明用户态配置无法解决；
17. 禁止无依据写 ES8388 register；
18. 禁止提前使用 FFmpeg/GStreamer 掩盖 ALSA 问题；
19. 原始日志与分析报告分开；
20. 每条命令注明在 RK3588 还是 PC 执行；
21. 发现目标过大时主动砍范围；
22. 不重复 V1 和 V2 已完成内容；
23. V2A 最后必须生成正式验收报告和 Git commit 建议。

现在开始帮助我完成 V2A。

你的第一轮回复必须严格按照以下 13 个主部分输出：

1. V2A 一句话目标
2. V2A 不做什么
3. V2A 最小可交付物
4. 推荐目录结构
5. Markdown 文档模板
6. ASoC / ALSA / ES8388 技术链路
7. 具体执行步骤
8. 推荐命令和完整代码设计
9. 最小验证实验
10. 常见坑和严格排查顺序
11. V2A 完成前自查清单
12. 延期时降级方案
13. 面试可讲点 + V2A 验收和下一阶段准入条件

第一轮不要猜测任何实际 ALSA card index、PCM device、ES8388 input pin、Capture Path、Capture Volume、sample rate、channel、period 或 buffer。

先从只读 ALSA/ASoC 环境盘点开始。

最终原则：

只有当“板载驻极体麦克风 → ES8388 → ASoC → ALSA PCM → 自研 C/C++ Capture → Timestamp → Sample Counter → 10 分钟稳定性测试”整条链路有完整证据后，才认为 V2A 完成。

V2A 完成不代表音视频同步已经完成。

真正的音视频同步、编码、mux、网络回传和 SD 卡录像必须留到 V8。
